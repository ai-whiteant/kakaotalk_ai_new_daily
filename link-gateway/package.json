{
  "name": "kakao-ai-news-link-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Allowlisted redirect gateway for Kakao ai news verified source links",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
